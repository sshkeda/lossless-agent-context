{
  "name": "@lossless-agent-context/e2e",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && printf 'ok\n' > dist/.build && tsc --noEmit -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --exclude src/real-log.test.ts",
    "test:real-logs": "vitest run src/real-log.test.ts"
  },
  "dependencies": {
    "@lossless-agent-context/adapters": "workspace:*",
    "@lossless-agent-context/core": "workspace:*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.112",
    "@mariozechner/pi-agent-core": "^0.67.5",
    "@mariozechner/pi-coding-agent": "^0.67.5"
  }
}
