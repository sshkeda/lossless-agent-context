{
  "name": "@lossless-agent-context/e2e",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && printf 'ok\n' > dist/.build && tsc --noEmit -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:real-logs": "LAC_ENABLE_REAL_LOG_E2E=1 vitest run src/real-log.test.ts"
  },
  "dependencies": {
    "@lossless-agent-context/adapters": "workspace:*",
    "@lossless-agent-context/core": "workspace:*",
    "@lossless-agent-context/projection-ai-sdk": "workspace:*",
    "@lossless-agent-context/projection-openinference": "workspace:*",
    "@lossless-agent-context/replay": "workspace:*"
  }
}
